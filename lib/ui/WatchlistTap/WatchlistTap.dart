import 'package:flutter/material.dart';

class WarchlistTap extends StatelessWidget {
  const WarchlistTap({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}